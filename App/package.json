{
  "scripts": {
    "eslint": "eslint './**/*.js'",
    "eslintfix": "eslint './**/*.js' --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  }
}
