// Paste your JavaScript code below to perform the complete analysis of its structure. ESLint will identify problematic patterns in the code by following the parameters configured in the .eslintrc.json file. The following code is just an example:

var num1, num2
let num3, num4

const array = [
    { Object1: "Object1" },
    { Object2: "Object2" }
]

try {
    if (num1 > num2) {
        console.log(num3)
    } else {
        console.log(num4)
    }
} catch (error) {
    console.log(error)
}

console.log(array)
